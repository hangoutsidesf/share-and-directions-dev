import React from 'react';

export default () => (
	<div>
		<h1>Instructions</h1> 
		<h2>
		Try to type an endpoint into the URL (this uses '/:parkId'). The real function of this dev app
		is to test out the Share and Get Directions buttons which has no place in '/'.
		</h2>
	</div>
);